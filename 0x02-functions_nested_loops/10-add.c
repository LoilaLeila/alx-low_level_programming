#include "main.h"
/**
 * add - a program that adds two integers
 * @a: parameter
 * @b: parameter
 * Return: (0) (Success)
 */
int add(int a, int b)
{
	int c;

	c = a + b;

		_putchar(c + '0');
	return (c);
}
