#include "main.h"
/**
 * _abs - shows the absolute number of an integer
 * @i: parameter
 * Return: i and -i
 */
int _abs(int i)
{
	if (i < 0)
	{
		return (-i);
	}
	else
	{
		return (i);
	}
	return (0);
}
